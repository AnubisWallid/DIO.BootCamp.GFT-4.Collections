git config --global user.name "John Doe"
git config --global user.email johndoe@example.com

git init // inicia o projeto

git clone git://github.com/AlgumaConta/ExemploProjeto.git //adicionando algum projeto do github a sua maquina local
git clone git://github.com/AlgumaConta/ExemploProjeto.git NewFolder // cria uma para com o nome "NewFolder"

git add //Grava alteracoes para depois serem adicionados ao Commit
git status // Verifica a atual situacao dos arquivos no projeto

git commit -m "Mensagem" // Faz o stage do projeto

git rm -f {arquivo} // Remove algum arquivo do projeto

git mv arquivo_origem arquivo_destino // Move arquivos
